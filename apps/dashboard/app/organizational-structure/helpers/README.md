# organizational-structure helpers

Utilities and calculations used **only** by the `app/[locale]/admin/organizational-structure` route.

- Put pure functions and small helper modules here.
- If a helper becomes shared across multiple routes or features, move it to the global `helpers/` folder and import it from there.


